import { FIXTURE } from '../../lib/url';

export const CREATE_FIXTURE = `${FIXTURE}`;
export const GET_FIXTURE = `${FIXTURE}`;
export const REMOVE_FIXTURE = `${FIXTURE}remove/`;
export const EDIT_FIXTURE = `${FIXTURE}/`;
export const SEARCH_FIXTURES = `${FIXTURE}/`;
